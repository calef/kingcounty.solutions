#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'mayhem/organizations/feed_updater'

if $PROGRAM_NAME == __FILE__
  Mayhem::Organizations::FeedUpdaterCLI.run
end
